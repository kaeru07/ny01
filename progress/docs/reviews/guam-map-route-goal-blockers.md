# グアム地図なぞり Goal — 再実行条件インベントリ

- goalId: `goal-mqq28l9d-8h86j`
- title: グアムアプリに、イベントごとの移動経路を地図でなぞって表示できるようにする
- epicId: `epic-goalstep-goal-mqq28l9d-8h86j-8h86j`（dispatch: `epic-goalstep-goal-mqq28l9d-8h86j`）
- 記録日時: 2026-07-07T05:02Z
- 前回失敗 runId: `20260707-110340-847`（`claude_rate_limited` / Codex 不可）

## 1. 結論

このディスパッチは「要修正・失敗の修正対応」だが、**コード上のバグは存在しない**。
前回の失敗は Claude 利用上限（`claude_rate_limited`）という一過性のインフラ制約であり、
握りつぶすべき症状も、差分で直せる不具合もない。

一方で、Goal 本体（地図なぞり表示）の実装は、上限リセットを待っても**このサンドボックスでは着手できない**。
構造的な前提（リポジトリアクセス・仕様・API 承認）が未充足のため。以下に再実行条件を整理する。

## 2. 実装をブロックしている構造的前提

### B1. リポジトリがサンドボックス外
- 本セッションのアクセス許可は `/root/company/apps/ny01/progress` のみ。
- `/root/company/apps/` 配下の `ls` / `find` はサンドボックスによりブロックされ、Guam アプリのディレクトリを列挙・編集できない。
- Guam アプリは別 clone（別 Git リポジトリ）であり、company CLAUDE.md の「アプリごとの作業はそのアプリのリポジトリ内で完結」原則にも合致する。
- → 実行者が Guam アプリのリポジトリを作業ディレクトリ内で読み書きできる状態が必要。

### B2. Goal 仕様が未定義
- `goal-mqq28l9d-8h86j` は `description` 空 / `taskPrompt` 空 / `doneCriteria` 空 / GitHub ターゲット・ブランチ指定なし。
- （対比）同じ goals.json の麻雀 Goal `goal-mqqmhcb9-jndlz` は repo・branch・Bundle ID・完了条件まで完全記述されている。この差が「着手できる Goal」と「できない Goal」を分けている。
- → 最低限の仕様が必要:
  - 対象リポジトリ URL とブランチ
  - イベント → 移動経路のデータモデル（座標列 / 経由地 / 順序）
  - 「地図でなぞる」の UI 定義（ポリライン描画か、実道路ルーティングか）
  - 完了条件（どの画面で、何を、どう検証するか）

### B3. 地図 / ルーティング API 方針が承認待ち（billing 抵触）
- 「移動経路を地図でなぞる」は地図プロバイダ + ルーティング/Directions API を前提とする（Google Directions / Mapbox / OSRM 等）。
- プロバイダ選定・API キーの扱い・課金発生の可否が未承認。
- 禁止事項「課金(billing)を発生させない」「Approval/Decision 待ちの作業を勝手に進めない」に該当するため、方針確定まで着手不可。
- → 必要な承認: 地図/ルーティング API プロバイダ + キー供給方法 + 課金受容の可否。

## 3. 再実行条件チェックリスト

すべて満たして初めて実装に着手できる:

- [ ] Claude 利用上限がリセット、または利用可能な executor が確保されている（一過性・時間経過で解消）
- [ ] Guam アプリのリポジトリが executor の作業ディレクトリ内でアクセス可能（URL + ブランチ）(B1)
- [ ] Goal に具体的な仕様が記入されている（taskPrompt / doneCriteria / データモデル / UI 定義）(B2)
- [ ] 地図 + ルーティング API 方針が承認済み（プロバイダ / キー / 課金受容）(B3)

## 4. 次にやるべきこと（人間側の判断待ち）

1. B3 の API 方針を承認 or 却下（承認必須事項。課金・外部 API に該当）。
2. B2 の Goal 仕様を記入（対象 repo / 完了条件 / データモデル）。
3. B1 の repo アクセスを executor に付与、または Guam アプリ側リポジトリで作業する dispatch に切り替え。
4. 上記が揃った時点で本 Epic を再ディスパッチ。上限起因の失敗は時間経過で自動的に再試行可能。
